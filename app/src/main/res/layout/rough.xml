<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Include the layout for MainActivity here -->
    <include layout="@layout/activity_recieve_before" />

    <!-- Include the layout for HomeActivity here -->
    <include layout="@layout/activity_reciceve_msg" />

</FrameLayout>
